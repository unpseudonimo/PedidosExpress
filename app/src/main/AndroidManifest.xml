<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PedidosExpress"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agrega la entrada para RepartidorActivity -->
        <activity android:name=".views.RepartidorActivity">
            <!-- Puedes agregar más configuraciones de actividad aquí si es necesario -->
        </activity>
        <!-- Agrega la entrada para ConsumidorActivity -->
        <activity android:name=".views.ConsumidorActivity">
            <!-- Otras configuraciones de actividad -->
        </activity>
        <!-- Agrega la entrada para AddCuenta -->
        <activity android:name=".views.AddCuenta">
            <!-- Otras configuraciones de actividad -->
        </activity>
        <!-- Agrega la entrada para AddCuenta -->
        <activity android:name=".views.Recuperacion">
            <!-- Otras configuraciones de actividad -->
        </activity>
        <activity android:name=".views.FeedConsumidor">
            <!-- Puedes agregar otros atributos de actividad aquí, como el nombre de la etiqueta, el tema, etc. -->
        </activity>
        <activity android:name=".views.FeedRepartidor">
            <!-- Puedes agregar otros atributos de actividad aquí, como el nombre de la etiqueta, el tema, etc. -->
        </activity>
        <activity android:name=".views.MapaActivity">
            <!-- Puedes agregar otros atributos de actividad aquí, como el nombre de la etiqueta, el tema, etc. -->
        </activity>
        <activity android:name=".views.Home">
            <!-- Puedes agregar más configuraciones si es necesario -->
        </activity>

    </application>
</manifest>
